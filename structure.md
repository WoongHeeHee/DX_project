# 가게제보 & 온보딩

## 화면명: 최초 화면

### 진입 조건
- 최초 웹앱 진입
- 누구나 접근 가능 (비회원 가능)

### 화면 구성
- 서비스명 로고
- 하단에 "google login" 버튼, "가게 제보하기" 버튼

### 사용자 액션
1. google login 버튼 클릭
    - 다음 화면: Google OAuth 로그인 화면
    - 전달 데이터: 없음

2. 가게 제보하기 버튼 클릭
    - 다음 화면: 가게 제보 가이드 화면
    - 전달 데이터: 없음

### 데이터 요구사항
- 없음

### 예외 처리
- 없음

---

## 제보 가이드 화면

### 진입 조건
- 최초 화면에서 "가게 제보하기" 선택 시

### 화면 구성
- 안내 문구 (프로젝트 설명)
- 제보 안내
- 촬영 시작 버튼

### 사용자 액션
1. "촬영 시작" 버튼 클릭
    - 버튼 클릭시 사용자에게 위치 & 촬영 권한 요청
    - 다음 화면: 카메라 촬영 화면
    - 전달 데이터: 없음

### 데이터 요구사항
- 없음

### 예외 처리
- 사용자가 권한 거부 시 "제보를 위한 위치/촬영 권한이 필요합니다. 설정에서 허용해주세요"

---

## (제보용) 카메라 촬영 화면

### 진입 조건
- 카메라, 위치 권한 허용됨

### 화면 구성
- 네이티브 카메라 UI
- 촬영 버튼

### 사용자 액션
1. 사진 촬영
    - 다음 화면: (제보용) 주변 가게 선택 화면
    - 전달 데이터: 촬영된 image, 현재 위치
    - 촬영된 사진은 자동으로 업로드

### 데이터 요구사항
- OS 카메라 권한 필요
- 위치권한 필요

### 예외 처리
- 없음

---

## (제보용) 주변 가게 선택 화면

### 진입 조건
- (제보용) 카메라 촬영 화면에서 사진 촬영

### 화면 구성
- 안내 문구
- 후보 가게 리스트 (3~5개, 사용자 촬영위치 기준 반경 5m 내 가게)
    - 리스트 내 항목에는 가게 이름 / 가게 사진이 있을 예정
- 선택 완료 버튼

### 사용자 액션
1. 가게 선택
    - 선택된 store_id 저장
2. 선택 완료 버튼 클릭
    - 다음 화면: 제보 완료 화면

### 데이터 요구사항
- 

### 비고
- 서버로 업로드 된 사진은 자체모델로 적정여부 판단
- 사용자가 어떤 가게를 찍었는지 판별하는건 위치기반
- project/src/image_processor.py 의 validate_store_photo() 참고
    - 해당 함수 수정필요
        1. 적합하고 가게사진인 경우
            - 해당 사진 삭제 (사용 안함)
        2. 적합하고 음식사진인 경우
            1. 여러 음식사진인 경우
                - 각 음식 detect후 따로 메뉴 태깅, 분류 (bbox별로 자름, 즉 여러개 메뉴가 찍힌 사진은 전부 하나의 메뉴 사진으로 나뉠예정)
                - 해당 나뉜 사진은 차후 "지도 - 시장 화면 (1)"에 사용될 예정
            2. 하나의 음식사진인 경우
                - 해당 음식 메뉴 태깅, 차후 "지도 - 시장 화면 (1)"에 사용될 예정
        3. 적합하지 않은 사진인 경우
            - 해당 사진 삭제

---

## 제보 완료 화면

### 진입 조건
- (제보용) 주변 가게 선택 화면에서 가게 선택 완료 후

### 화면 구성
- 제보 완료 감사 문구
- 최초화면 돌아가기 버튼

### 사용자 액션
1. 최초화면 돌아가기 버튼 클릭
    - 다음 화면: 최초화면

### 데이터 요구사항
- 없음

### 예외 처리
- 없음

---

## 온보딩 진입 화면

### 진입 조건
- 최초 화면에서 google login 버튼 클릭 후 로그인
- 최초 로그인 후 온보딩을 완료하지 않음

### 화면 구성
- 한국어, 영어, 중국어, 일본어 선택 화면
- 하단에 "선택 완료" 버튼

### 사용자 액션
1. 언어 선택
2. 선택완료 버튼 클릭
    - 다음 화면: 온보딩 - 이름입력화면
    - 전달 데이터: 사용자의 언어

### 데이터 요구사항
- user email

### 예외 처리
- 언어 미선택 시 "선택 완료" 버튼 비활성화화

---

## 온보딩 - 이름입력화면

### 진입 조건
- 온보딩 진입 화면에서 언어 선택 후 "선택 완료" 버튼 클릭

### 화면 구성
- 상단에 안내문구
- 중단에 이름 입력란 존재
- 입력란 옆 "확인" 버튼

### 사용자 액션
1. 이름 입력란에 이름 입력(본인 모국어)
2. "확인" 버튼 클릭
    - 다음 화면: 이름 생성 대기화면
    - 전달 데이터: user name

### 데이터 요구사항
-

### 예외 처리
- 입력한 이름이 언어가 아닌 특수문자만 존재하는 경우, "올바른 성함을 입력해주세요"

---

## 온보딩 - 이름생성 대기화면

### 진입 조건
- 온보딩 - 이름입력화면에서 이름 입력 후 "확인" 버튼 클릭

### 화면 구성
- 상단에 안내문구
- 중단에 로딩표시
- 로딩표시 내부에 "Choosing a korean name just for ... \n {user name}" 문구
    - (로딩 중)자체 언어생성 모델(project/src/korean_name_generator.py 참고)통해 사용자 한국이름 생성
- (생성 완료 후) 중단 로딩표시 및 로딩표시 내부 문구 사라지고 "{한국이름}님\n{영어발음}" 문구

### 사용자 액션
1. 없음 (생성 대기)

### 데이터 요구사항
-

### 예외 처리
- 없음 (온보딩 - 이름입력화면에서 처리)

---

## 온보딩 - 메인화면 (1)

### 진입조건
- 온보딩 - 이름생성 대기화면에서 넘어옴

### 화면 구성
- 상단 문구
- 중단에 국적 선택란, 나이선택(yyyy.mm)란, 맵기 선택란(categorical, 5단계)
- 전부 선택 시 하단에 next page 버튼 생성

### 사용자 액션
1. 정보 입력
    - 전달 데이터: user nationality, user birth, user spicy_level
2. "next page" 버튼 클릭
    - 다음 화면: 온보딩 - 메인화면 (2)

### 데이터 요구사항
-

### 예외 처리
- 없음

---

## 온보딩 - 메인화면 (2)

### 진입 조건
- 온보딩 - 메인화면 (1)에서 선택 후 "next page" 버튼 클릭

### 화면 구성
- 상단에 안내문구
- 중단에 메뉴 도전강도 선택 (안정형, 도전형, 몰라요), 한식경험 질문 (뉴비, 즐겜러, 고인물)
- 전부 선택 시 하단에 환영 문구 및 "Enter" 버튼생성

### 사용자 액션
1. 정보 입력
    - 전달 데이터: user challenge_level, user kfood_level
2. "Enter" 버튼 클릭
    - 다음 화면: 지도(홈)

### 데이터 요구사항
-

### 예외 처리
- 없음 (유저는 필수적으로 온보딩을 마쳐야함)

---
# (온보딩 이후부턴 하단에 항상 네비게이터가 있을 예정. (탐색/지도(홈)/카메라/마이)로 나뉨)
---
# 탐색 탭

## 탐색 화면

### 진입 조건
- 로그인 및 온보딩 완료 + 하단 네비게이터 중 "탐색" 클릭

### 화면 구성
- Scrollable한 페이지
- 최상단에 국적-나이 별 트렌드 메뉴 추천리스트, 개인 맞춤 추천리스트 (자세한 추천 알고리즘은 비고에)
    1. 국적-나이 별 메뉴 추천리스트
        1. 사용자는 해당 화면 최초 진입시 본인의 user nationality, user birth(년도 기준) top 3 메뉴를 볼 수 있음
        2. 사용자는 국적 / 나이를 바꿔가며 타 국적과 나이대의 top 3 메뉴를 볼 수 있음
        3. 해당 메뉴는 옆으로 넘기며 볼 수 있고, 랭킹 넘버는 별도로 나오지 않음
    2. 개인 맞춤 추천리스트
        1. 사용자가 "좋아요"를 누른 메뉴를 토대로 같은 메뉴를 "좋아요"를 누른 사람중 "좋아요"를 많이 받은 다른 메뉴 top 3를 볼 수 있음
        2. 사용자가 "좋아요"를 누른 메뉴가 없다면, 모든 메뉴 통틀어 최고 메뉴 top3를 노출
- 그 밑엔 "Korean Street Food" 구역이 있음
    1. Meals, Snacks, Sweets, Drink 카테고리가 있으며, 사용자는 이중 선택 가능 (카테고리는 추가 예정)
        1. 디폴트는 Meals
    2. 사용자가 카테고리를 고르면 그 밑에 해당하는 메뉴 사진 노출
- "Korean Street Food" 구역 밑엔 "Discover 시장" 구역이 있음
    1. 서울, 부산, 제주도, 인청 카테고리가 있으며, 사용자는 이중 선택 가능 (카테고리는 추가 예정)
        1. 디폴트는 서울
    2. 사용자가 카테고리를 고르면 그 밑에 해당하는 지역의 시장 노출

### 사용자 액션
1. top 3 추천 리스트
    1. 국적-나이 별 트렌드 메뉴 탭의 국적, 나이 조정
        - 전달 데이터: top 3 메뉴의 이름과 사진
    2. 메뉴 사진 클릭
        - 다음 화면: 메뉴 설명 페이지
2. "Korean Street Food" 구역
    1. 메뉴 카테고리 클릭
        - 전달 데이터: 해당하는 메뉴 사진 및 이름
    2. 메뉴 사진 클릭
        - 다음 화면: 메뉴 설명 페이지
3. "Discover Sijang" 시장 구역
    1. 시장 지역 클릭
        - 전달 데이터: 해당하는 지역의 시장 사진 및 이름
    2. 시장 사진 클릭
        - 다음 화면: 시장 설명 페이지

### 데이터 요구사항
- 

### 예외 처리
-

### 비고
- 추천 알고리즘은 project/backend/app/servies/recommendation_service.py 참고 (수정 필요함)
    1. 추천의 기준은 "좋아요"가 기준이며, "좋아요"는 마이>리뷰 페이지에서 다룰 예정
    2. 현재 recommendation_service.py 파일은 유저의 메타데이터 중 spicy_level, challenge_level, kfood_level도 활용하지만, 현재 서비스는 nationality와 birth만 활용해 추천할 예정
        1. 다른 메타데이터는 차후 연구용으로만 활용할 예정임. 즉, py파일 수정 필요
        2. birth는 10대, 20대, 30대, ..., 70대, 80+ 으로 나눠서 계산

---

## (메뉴별) 메뉴 설명 페이지

### 진입 조건
1. "탐색" 화면
    - top 3 추천리스트의 메뉴 사진 클릭
    - "Korean Street Food"의 메뉴 사진 클릭
2. "지도(홈)" 화면 (하단에 상세세 설명)
    - "시장 Bestselling"의 메뉴 사진 클릭
    - {시장이름} now의 메뉴 사진 밑 "음식 설명보기" 버튼 클릭

### 화면 구성
- Scrollable한 페이지
- 최상단엔 뒤로가기 버튼, 메뉴명, "찜하기" 버튼
- 그 밑엔 메뉴 사진
    - 대표사진 3개 등록 예정. 옆으로 스와이프해 탐색 가능
- 메뉴 사진 밑 메뉴 설명 구역
    - "{메뉴이름} \n {메뉴설명}"
- 메뉴 설명 구역 밑 비슷한 음식 구역
    - "비슷한 음식 \n {비슷한 음식}
- 비슷한 음식 구역 밑 맵기 구역
    - menu_items.spice_level에 따른 bar
- 맵기기 구역 밑 알레르기 안내 구역
    - "알레르기 안내 \n Contains\n{메뉴알러지}\n May Contain\n{메뉴may알러지}

### 사용자 액션
1. "뒤로가기" 버튼 클릭
    - 다음 화면: 이전화면
2. "찜하기" 버튼 클릭
    - 사용자-메뉴여부 추가
3. 음식 사진 구역 스와이프
    - 음식사진 탐색가능

### 데이터 요구사항
-

### 예외 처리
-

### 비고
- 메뉴 대표사진, 메뉴 설명, 메뉴별 비슷한 음식, 메뉴별 맵기, 알러지는 직접 미리 입력해놓을 계획

---

## (시장별) 시장 설명 페이지

### 진입 조건
1. "탐색 화면"
    - "Discover 시장" 구역의 시장 사진 클릭

### 화면 구성
- Scrollable한 페이지
- 최상단엔 뒤로가기 버튼
- 그 밑엔 시장 사진
    - 대표사진 3개 등록 예정. 옆으로 스와이프해 탐색 가능
- 시장 사진 밑 시장 설명 구역
    - "{시장이름}\n{시장설명}"
- 시장 설명 구역 밑 "꼭 먹어야할 것" 구역
    - 시장 별 Musteat 메뉴 3개 사진과 이름 노출
    - 해당 Musteat 메뉴는 하드코딩 예정
- "꼭 먹어야할 것" 구역 밑 "오시는 길" 구역
    - 지도 API로 위치 확인

### 사용자 액션
1. "뒤로가기" 버튼 클릭
    - 다음 화면: 이전화면
2. 꼭 먹어야할 것 구역 메뉴 사진 클릭
    - 다음 화면: 해당 메뉴의 메뉴 설명 페이지

### 데이터 요구사항
-

### 예외 처리
-

### 비고

---
# 지도

## 지도 - 시장선택 화면

### 진입 조건
- 로그인 및 온보딩 완료
- 하단 네비게이터 중 "지도" 클릭

### 화면 구성
- 상단 "사진이나 텍스트로 궁금한 메뉴를 물어보세요" 버튼
- 중단에 랜드마크가 이미 표시된 서울 실루엣
    - 그 위에 현재 위치 표기 (정적인 서울 실루엣 png위에 현재 위치를 찍을 수 있는지?)
- 하단에 스크롤해서 시장을 선택할 수 있는 구역
    - 시장은 광장시장, 통인시장, 망원시장이 있으며 (서울기준, 현재는 서울만 구현)
    - 스크롤해서 해당 시장이 하이라이팅 될 때마다 지도 위 실루엣에 포인트 확대
    - 각 시장 스크롤은 Keyword와 Address 존재
    - 시장 스크롤 우측 상단에는 상태 표시 색상이 있으며, marketinfo의 open_time, close_time활용 (녹,적색)

### 사용자 액션
1. "탐색" 버튼 클릭
    다음 화면: 탐색 화면
2. 시장 선택 구역
    1. 시장 목록 스크롤
    2. 시장 목록 클릭
        다음 화면: 지도 - 시장 화면

### 데이터 요구사항
-

### 예외 처리
-

### 비고
- 시장의 Address는 미리 입력해놓을 예정
- 시장의 Keyword는 차후 마이>다이어리 화면에서 사용자가 선택한 top 2 키워드 노출
    - 키워드는 정해져있음

---

## 검색 화면

### 진입 조건
- "지도 - 시장선택" 화면에서 "검색" 버튼 클릭

### 화면 구성
- 최상단 좌측에 "뒤로가기" 버튼
- 상단에 안내문구
- 아래에 "사진으로 찾기", "텍스트로 찾기" 버튼

### 사용자 액션
1. "뒤로가기" 버튼 클릭
    - 다음 화면: 지도 - 시장선택 화면
2. "사진으로 찾기" 버튼 클릭
    - 사용자 문서에 접근, 갤러리의 사진 선택해 업로드
    - 다음 화면: 사진 > 텍스트 입력 화면
3. "텍스트로 찾기" 버튼 클릭
    - 다음 화면: 텍스트 입력 화면

### 데이터 요구사항

### 예외 처리


### 비고
- project/src/menu_matcher.py의 match_menu() 참고

---

## 사진 > 텍스트 입력 화면

### 진입 조건
- "검색 화면"에서 "사진으로 찾기" 클릭 후 사진 업로드

### 화면 구성
- 최상단 좌측에 "뒤로가기" 버튼
- 상단 안내문구
- 아래 사용자 업로드한 사진
- 아래 텍스트 입력란 존재
- 그 아래 "메뉴 찾기" 버튼

### 사용자 액션
1. "뒤로 가기" 버튼 클릭
    - 다음 화면: 검색 화면
2. 텍스트 입력 (선택)
3. "메뉴 찾기" 버튼 클릭
    - 다음 화면: (메뉴별) 검색 결과 화면

### 예외 처리
- "메뉴 찾기" 버튼을 클릭한 뒤, 모델이 메뉴를 찾지 못했다면, "검색 실패 화면"으로 이동

---

## 텍스트 입력 화면

### 진입 조건
- "검색 화면"에서 "텍스트로 찾기" 클릭

### 화면 구성
- 최상단 좌측에 "뒤로가기" 버튼
- 상단 안내문구
- 아래 텍스트 입력란 존재
- 그 아래 "메뉴 찾기" 버튼

### 사용자 액션
1. "뒤로 가기" 버튼 클릭
    - 다음 화면: 검색 화면
2. 텍스트 입력 (필수)
3. "메뉴 찾기" 버튼 클릭
    - 다음 화면: (메뉴별) 검색 결과 화면

### 예외처리
1. 사용자가 텍스트를 미입력한 뒤 "메뉴 찾기" 버튼을 누를 경우, "메뉴에 대한 설명을 작성해주세요🖐️" 팝업 알림
2. "메뉴 찾기" 클릭 후, 모델이 메뉴를 찾지 못했다면 "검색 실패 화면"으로 이동동

---

## (메뉴별) 검색 결과 화면

### 진입 조건
- "사진 > 텍스트 입력 화면"에서 "메뉴 찾기" 클릭 후, 모델이 올바른 메뉴를 매칭
- "텍스트 입력 화면"에서 텍스트입력, "메뉴 찾기" 클릭 후, 모델이 올바른 메뉴를 매칭

### 화면 구성
- 화면 최상단 좌측 "뒤로 가기" 버튼
- 상단 안내 문구
- 그 밑 메뉴 대표사진
- 그 아래 메뉴 이름, 설명
- 메뉴 이름 옆 "찜하기" 버튼
- 하단 "가장 가까운 {시장이름} {메뉴이름} 가게 보러가기" 버튼

### 사용자 액션
1. "찜하기" 버튼 클릭
    - 사용자-메뉴여부 추가
2. "가장 가까운 {시장이름} {메뉴이름} 가게 보러가기" 버튼 클릭
    다음 화면: 지도 - 시장 화면 (2) (하단 설명)
3. "뒤로 가기" 버튼 클릭
    다음 화면: 검색 화면

### 데이터 요구사항
- 사용자 위치

### 예외 처리

### 비고
- {시장이름} {메뉴이름} 보러가기에서 메뉴는 "탐색 화면"에서 쓰이는 모델의 output, 시장은 현재 사용자와 가장 가까운 가게가 속해있는 시장

---

## 검색 실패 화면

### 진입 조건
- "사진 > 텍스트 입력 화면"에서 "메뉴 찾기" 클릭 후, 모델이 메뉴 매칭 실패
- "텍스트 입력 화면"에서 텍스트입력, "메뉴 찾기" 클릭 후, 모델이 메뉴 매칭 실패

### 화면 구성
- 최상단 좌측 "뒤로가기" 버튼
- 상단 안내문구
- 그 밑 "다시 입력하기" 버튼

### 사용자 액션
1. "뒤로가기" 버튼 클릭
    - 다음 화면: 검색 화면
2. "다시 입력하기" 버튼 클릭
    - 다음 화면: 검색 화면

---

## 지도 - 시장 화면 (1) (map-market_page (1).png 참고)

### 진입 조건
- 지도 - 시장선택 화면에서 시장 선택(클릭)

### 화면 구성
- 최상단 좌측에 뒤로가기 버튼
- 상단에 선택한 시장의 지도 출력 (지도 API)
    - 지도위에는 해당 시장에서 최근에 올라온 사진의 위치가 핀으로 찍혀있음
    - 상위 10개 위치, 60분 전 데이터들 포인팅
- 아래에 바텀시트 존재
    - 바텀시트는 Now와 Saved가 존재하며, Now의 항목은 해당 시장의 메뉴를 보고, 그 메뉴를 고르면 메뉴를 파는 가게를 pin하는 형식. (pin된 가게는 Saved에서 볼 수 있음)
    - Must eat {시장이름} 문구 존재재, 해당 음식의 사진과 메뉴 노출 (클릭 불가)
    - Bestselling 밑에는 "{시장 이름} now"가 존재하며, (제보용) 카메라 촬영 화면과 (리뷰용) 카메라 촬영 화면에서 업로드 된 사진이 업로드 시점에서 가장 가까운 것들로 나열됨. 사진은 옆으로 swipe하며 넘길 수 있음. 사진에는 "{현재시간 - 업로드시간}분 전" 존재
    - "{시장 이름} now" 영역의 사진 위에는 카테고리 버튼이 존재하며(전체보기, 식사, 간식, 디저트, 음료수), default는 전체보기.
    - 사진 밑에는 "{메뉴이름} 메뉴 설명 보기" 버튼 존재
        - 메뉴는 하이라이팅 되고있는 사진 메뉴
    - 그 밑 "{시장이름} 안 {메뉴이름} 가게 보기"
        - 메뉴는 하이라이팅 되고있는 사진 메뉴, 시장은 사용자가 선택한 시장

### 사용자 액션
1. 뒤로가기 버튼 클릭
    - 다음 화면: 지도 - 시장선택 화면
2. "{시장 이름} now" 영역의 카테고리 클릭
    - 해당 메뉴의 type에 속하는 최근 업로드 된 사진 노출
3. "{시장 이름} now" 영역의 사진 swipe
    - 사진을 넘기며 탐색
    - 하이라이팅 되는 사진의 위치는 상단 지도의 핀 하이라이팅
4. "{메뉴이름} 메뉴 설명 보기" 버튼 클릭
    다음 화면: (메뉴별) 메뉴 설명 페이지
5. "{시장이름} 안 {메뉴이름} 가게 보기" 버튼 클릭
    다음 화면: 지도 - 시장 화면 (2)
6. "Saved" 버튼 클릭
    다음 화면: 지도 - 시장 화면 (3)

### 데이터 요구사항
-

### 예외 처리
-

### 비고
- 지도 - 시장 화면 (1, 2, 3)은 상단 지도 출력은 유지하되, 바텀시트 내 내용이 바뀜
- "최근 업로드 된 사진"은 두가지 경우 존재
    1. 가게 제보하기
    2. 마이 > 리뷰 업로드 (리뷰 관련 이야기는 후술)

---

## 지도 - 시장 화면 (2)

### 진입 조건
1. 지도 - 시장 화면 (1)에서 "{시장이름} 안 {메뉴이름} 가게 보기" 버튼 클릭
2. (메뉴별) 검색 결과 화면에서 "가장 가까운 {시장} {메뉴명} 가게 보러가기" 버튼 클릭

### 화면 구성
- 최상단 좌측에 뒤로가기 버튼
- 상단에 선택한 시장의 지도 출력 (지도 API)
    - 지도위에는 바텀시트에 나열되어있는 리스트의 가게 위치가 핀으로 찍혀있음
- 아래에 바텀시트 존재
    - "{메뉴이름} in {시장이름}" 아래로 가게 리스트 나열
    - 가게 리스트 구성요소 (요소별 하이라이팅 X, Scrollable)
        1. 가게 사진 - 대표 사진 저장예정
        2. 가게명
        3. "위치 보기" 버튼 - 클릭 시 상단 지도 중앙에 해당 가게 위치
        4. "핀 하기" 버튼 - 클릭 시 사용자-핀 여부 추가
        5. 현재 상태 - 녹색, 황색, 적색 존재. 비고에 후술

### 사용자 액션
1. "위치 보기" 버튼 클릭
    - 상단 지도 중앙에 해당 가게 위치
2. "핀 하기" 버튼 클릭
    - 사용자-핀 여부 추가
3. "뒤로가기" 버튼 클릭
    - 다음 화면: 지도 - 시장 화면 (1)

### 데이터 요구사항
-

### 예외 처리
-

### 비고
- 현재 상태 표기 기준
    - 기본적으로 휴무일(closed_days)에는 적색. 휴무 요일이 문자열로 나열될 예정 (ex. "월요일, 수요일")
    - 해당 가게에 제보가 들어온 경우 (리뷰용 사진 X)
        - 초록색: (오픈시간, 마감시간 - 15m)
        - 황색: (마감시간 - 15m, 마감시간)
        - 적색: (,오픈시간) | (마감시간,)
    - 해당 가게에 제보가 들어오지 않은 경우
        - 초록색: (오픈시간, 마감시간 - 30m)
        - 황색: (마감시간 - 30m, 마감시간 - 15m)
        - 적색: (, 오픈시간) | (마감시간 - 15m, )
- 해당 화면에는 NOW, SAVED가 없음

---

## 지도 - 시장 화면 (3)

### 진입 조건
1. 지도 - 시장 화면 (1)에서 "Saved" 버튼 클릭

### 화면 구성
- 최상단 좌측에 뒤로가기 버튼
- 상단에 선택한 시장의 지도 출력 (지도 API)
- 아래에 바텀시트 존재
    "{시장이름} 내 저장 목록" 영역 존재
    1. 핀한 가게가 없을 경우
        안내 문구 ("Now에 가서 음식을 둘러보고 가게를 저장해 리스트를 채워보세요!")
    2. 핀한 가게가 있을 경우
        - "{시장이름} 내 저장 목록" 영역 밑 핀한 가게 리스트 나열
        - 핀한 가게 리스트 구성요소
            1. 가게 사진
            2. 가게명
            3. 가게 영업시간
            4. 가게 휴무일
            6. "위치 보기" 버튼
            7. "핀 해제" 버튼
            8. 현재 상태

### 사용자 액션
1. "위치 보기" 버튼 클릭
    - 상단 지도 중앙에 해당 가게 위치
2. "핀 해제" 버튼 클릭
    - 핀 리스트에서 제거
3. "Now" 버튼 클릭
    다음 화면: 지도 - 시장 화면 (1)
4. "뒤로 가기" 버튼 클릭
    다음 화면: 지도 - 시장 화면 (1)

### 데이터 요구사항
-

### 예외 처리
-

### 비고
- 가게 사진, 가게명, 가게 영업시간, 가게 휴무일은 DB에 업로드 예정
- 현재 상태 로직은 "지도 - 시장 화면 (2)"와 동일

---

# 카메라

## 리뷰 사진 촬영

### 진입 조건
- 사용자가 "시장 내에 있을 때" 네비게이터 "카메라" 탭 활성화
- 활성화 되어있을 때 "카메라" 탭 클릭

### 화면 구성
- 네이티브 카메라 UI
- 촬영 버튼

### 사용자 액션
1. 음식 사진 촬영
    다음 화면: (리뷰용) 주변 가게 선택 화면
    데이터: 촬영된 image, 현재 위치

### 데이터 요구사항
- OS 카메라 권한 필요
- 위치권한 필요

### 예외 처리
-

### 비고
- 

---

## (리뷰용) 주변 가게 선택 화면
(해당 화면은 기획, 디자인에는 없었으나 기능 상 필요할 것 같아 임의로 작성함)

### 진입 조건
- 리뷰 사진 촬영 화면에서 사진 촬영

### 화면 구성
- 안내 문구
- 후보 가게 리스트 (3~5개, 사용자 촬영위치 기준 반경 5m 내 가게)
    - 리스트 내 항목에는 가게 이름 / 가게 사진이 있을 예정
- 선택 완료 버튼

### 사용자 액션
1. 가게 선택
    - 선택된 store_id 저장
2. 선택 완료 버튼 클릭
    - 다음 화면: 제보 완료 화면

### 데이터 요구사항
- 

### 비고
- 서버로 업로드 된 사진은 자체모델로 적정여부 판단
- 사용자가 어떤 가게를 찍었는지 판별하는건 위치기반
- project/src/image_processor.py 의 validate_store_photo() 참고
    - 해당 함수 수정필요
        1. 적합하고 가게사진인 경우
            - 해당 사진 삭제 (사용 안함)
        2. 적합하고 음식사진인 경우
            1. 여러 음식사진인 경우
                - 각 음식 detect후 따로 메뉴 태깅, 분류 (bbox별로 자름, 즉 여러개 메뉴가 찍힌 사진은 전부 하나의 메뉴 사진으로 나뉠예정)
                - 해당 나뉜 사진은 차후 "지도 - 시장 화면 (1)"에 사용될 예정
            2. 하나의 음식사진인 경우
                - 해당 음식 메뉴 태깅, 차후 "지도 - 시장 화면 (1)"에 사용될 예정
        3. 모든 음식 사진은 다이어리 페이지에서 쓰일 예정

---

# 마이
(마이 관련 화면 및 기획은 아직 미완입니다. 존재하는 것만 우선 적습니다)

## 마이페이지 화면

### 진입 조건
- 하단 네비게이터 "마이" 클릭

### 화면 구성
- 최우측상단에 "설정" 버튼
- 상단 중앙에 사용자 프로필사진 (원형)
- 프로필 사진 밑 사용자 한국 이름 및 영어발음 (온보딩 - 이름생성 대기화면 참고)
- 아래 총 5개의 컨테이너 존재
    1. 리뷰(다이어리) 작성페이지
        1. 리뷰용으로 촬영한 음식 사진이 없는 경우
            - 방문 유도 문구와 "Let's go" 버튼 존재
        2. 리뷰용으로 촬영한 음식 사진이 있는 경우
            - 다이어리 작성 유도 문구와 "okay" 버튼 존재
    2. 핀한 가게 컨테이너(버튼)
    3. 저장한 메뉴 컨테이너(버튼)
    4. 메뉴 컨테이너
        사용자가 리뷰용으로 촬영한 음식의 메뉴 나열 (메뉴별 아이콘 나열 예상)
    5. 시장 컨테이너
        사용자가 리뷰용으로 촬영한 시장 나열

### 사용자 액션
1. "설정" 버튼 클릭
    다음 화면: 설정 화면
2. (리뷰가 없는 경우) "Let's go" 버튼 클릭
    다음 화면: 지도 - 시장선택 화면
3. (리뷰가 있는 경우) "okay" 버튼 클릭
    다음 화면: 다이어리 화면
4. "핀한 가게" 버튼 클릭
    다음 화면: 마이>핀한 가게 화면
5. "저장한 메뉴" 버튼 클릭
    다음 화면: 마이>저장한 메뉴 화면

### 데이터 요구사항
-

### 예외 처리
- 사용자 프로필 사진 미업로드시 회색 원으로 placeholder

### 비고

---

## 설정 화면

### 진입 조건
- 마이페이지 화면에서 "설정" 버튼 클릭

### 화면 구성
- 최상단 좌측에 "뒤로가기" 버튼
- 계정 섹션 "프로필 수정" 버튼
- 그 밑 사용자 관리 섹션 "1:1 문의사항", "공지사항 및 이용약관" 버튼
- 하단에 서비스명 및 "로그아웃" 버튼

### 사용자 액션
1. "뒤로가기" 버튼 클릭
    다음 화면: 마이페이지 화면
2. "프로필 수정" 버튼 클릭
    다음 화면: 프로필 수정 화면
3. "1:1 문의사항" 버튼 클릭
    다음 화면: 1:1 문의사항 화면
4. "공지사항 및 이용약관" 버튼 클릭
    다음 화면: 공지사항 및 이용약관 화면
5. "로그아웃" 버튼 클릭
    다음 화면: 최초 화면
    current_user 초기화

### 데이터 요구사항

### 예외 처리

### 비고

---

## 프로필 수정 화면

### 진입 조건
- 설정 화면에서 "프로필 수정" 버튼 클릭

### 화면 구성
- 최상단 좌측에 "뒤로가기" 버튼
- 상단 이름 수정란, "새로받기" 버튼 존재
- 중앙 언어 설정 / 나이 / 맵기 레벨 / 도전 의향 / 한식 경험 변경 란
- 하단 구글 계정 변경 버튼 (구현에 어려움이 있을경우 기각)
- 하단에 "완료" 버튼

### 사용자 액션
1. "뒤로가기" 버튼 클릭
    다음 화면: 설정 화면
2. 새 이름(자국어) 입력 후 "새로받기" 버튼 클릭
    scr/korean_name_generator.py 모델 활용, 새로운 한국 이름 생성
3. 사용자 온보딩 데이터 수정
4. "완료" 버튼 클릭
    수정된 데이터 적용

### 데이터 요구사항

### 예외 처리

### 비고

---

## 1:1 문의사항 화면

### 진입 조건
- 설정 화면에서 "1:1 문의사항" 버튼 클릭

### 화면 구성
- 최상단 좌측에 "뒤로가기" 버튼
- "많이 묻는 QnA" 섹션과 "문의사항" 섹션 존재
    1. "많이 묻는 QnA" 섹션
        질문 리스트가 존재, 사용자는 리스트 클릭 시 해당 질문에 대한 답이 펼쳐지며 볼 수 있음
        (질문 리스트와 답은 미리 준비)
    2. "문의 사항" 섹션
        사용자는 텍스트를 입력하는 공간이 있음
        입력공간 밑에는 "upload" 버튼

### 사용자 액션
1. "뒤로가기" 버튼 클릭
    다음 화면: 설정화면
2. 공용질문 리스트 클릭
    펼쳐지며 답변 노출
3. 문의사항 입력 후 "upload" 버튼 클릭
    해당 문의 데이터 저장

### 데이터 요구사항

### 예외 처리
- 문의내용 미입력 시 upload 버튼 비활성화

### 비고

---

## 공지사항 및 이용약관 화면 (프론트에서 해결할 예정)

### 진입 조건
- 설정 화면에서 "공지사항 및 이용약관" 버튼 클릭

### 화면 구성

### 사용자 액션

### 데이터 요구사항

### 예외 처리

### 비고

---

## 다이어리 화면

### 진입 조건
- 사용자는 (리뷰용) 주변 가게 선택 화면을 최소 한번은 거쳐야함
- 마이페이지 화면에서 "okay" 버튼 클릭

### 화면 구성
- 최상단 좌측에 "뒤로가기" 버튼
- 상단에 {한국이름}님과 __시장 Story 문구
    - __시장의 __란은 터치하여 선택 가능
    - 선택 가능한 시장은 사용자가 "완료"하기 전 리뷰(사진)을 올린 시장
- 문구 밑 키워드를 선택할 수 있는 카테고리 버튼 존재
    - 버튼 목록: "대부분 현지인들이 방문해요", "대부분 관광객들이 방문해요", "한적해요", "내부가 넓어요", "가게가 협소해요", "주차하기 좋아요", "대중교통이 편해요", "사람들이 친절해요"
    - 선택된 키워드는 지도 - 시장 선택화면에 활용
- 아래에 {한국이름}님의 메뉴판 문구
- 문구 밑에는 사용자가 찍은 음식사진을 detect한 결과 나열
    - 음식은 __시장을 선택했을 때, 그 시장에서 먹은 것만 나열
        ex. 사용자가 다이어리를 "완료" 하기 전 데이터: 망원시장에서 떡볶이, 호떡을 먹고(사진을 찍고) 통인시장에서 호떡, 순대를 먹었다면(사진을 찍었다면), 상단에서 "망원"을 골랐을 경우 아래엔 떡볶이, 호떡이 나열
    - 각 사진(메뉴) 밑에는 "좋아요" 버튼
- 하단에 "완료" 버튼

### 사용자 액션
1. "뒤로가기" 버튼 클릭
    다음 화면: 마이페이지 화면
2. 시장 선택
    선택할 때 마다 하단 메뉴 리스트 변경
3. 키워드 선택
4. 메뉴별 좋아요
5. "완료"버튼 클릭
    - 시장별 키워드, 시장-메뉴별 좋아요 데이터 저장
    다음 화면: 마이페이지 화면

### 데이터 요구사항

### 예외 처리
- 사용자가 키워드를 하나도 안고르고 "완료" 버튼을 누른 경우: "최소 하나의 키워드를 선택해주세요"
- 사용자가 좋아요를 하나도 안고르고 "완료" 버튼을 누른 경우: "최소 하나의 메뉴에 좋아요를 눌러주세요"

### 비고
- "완료"된 다이어리를 보는 페이지는 아직 미기획입니다.

---

## 마이>핀한 가게 화면

### 진입 조건
- 마이페이지에서 "핀한 가게" 클릭

### 화면 구성
- 최상단 좌측에 "뒤로가기" 버튼
- 중단엔 핀한 가게 리스트
    1. 핀한 가게가 없는 경우
        "아직 맛있는 걸 못찾으셨나요?" 문구 아래 "찾으러 가기" 버튼
    2. 핀한 가게가 있는 경우
        - 핀한 가게 리스트 위 시장 선택 가능 (전체, 망원시장, 광장시장 등)
        - 핀한 가게 리스트 구성요소
            1. 가게 사진
            2. 가게 이름
            3. "위치보기" 버튼
            4. "핀 해제" 버튼

### 사용자 액션
1. "뒤로가기" 버튼 클릭
    다음 화면: 마이페이지 화면
2. "찾으러 가기" 버튼 클릭
    다음 화면: 지도 - 시장 선택화면
3. 시장 선택
    선택한 시장 별 하단 핀 리스트 정렬
4. "위치보기" 버튼 클릭
    다음 화면: 지도 - 시장 화면 (3) + 상단 지도 중앙을 해당 가게로
5. "핀 해제" 버튼 클릭
    유저-핀 해제

### 데이터 요구사항

### 예외 처리

### 비고

---

## 마이>저장한 메뉴 화면

### 진입 조건
- 마이페이지에서 "저장한 메뉴" 클릭

### 화면 구성
- 최상단 좌측에 "뒤로가기" 버튼
- 중단엔 저장한 메뉴 리스트
    1. 저장한 메뉴가 없는 경우
        "아직 맛있는 걸 못찾으셨나요?" 문구 아래 "찾으러 가기" 버튼
    2. 저장한 메뉴가 있는 경우
        상단에 "저장한 메뉴 {num_저장메뉴}개" 문구
        본문 내용에 메뉴 사진/이름이 3xn으로 scrollable하게 나열

### 사용자 액션
1. "뒤로가기" 버튼 클릭
    다음 화면: 마이페이지 화면
2. 메뉴 사진 클릭
    다음 화면: (해당 메뉴) 메뉴 설명 페이지

### 데이터 요구사항

### 예외 처리

### 비고